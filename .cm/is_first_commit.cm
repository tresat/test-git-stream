# -*- mode: yaml -*-
manifest:
  version: 1.0
automations:
  first_commit:
    if:
      - {{ repo.contributors | isFirstCommit(branch.author) }}
    run:
      - action: add-comment@v1
        args:
          comment: Welcome {{branch.author}}!