# -*- mode: yaml -*-
manifest:
  version: 1.0

automations:
  # If someone adds a TODO, remind them about it by requesting removal
  point_out_todos:
    if:
      - {{ true }}
    run:
      - action: add-comment@v1
        args:
          comment: {{ env.team1 }}